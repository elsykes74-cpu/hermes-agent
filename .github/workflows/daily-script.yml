name: Elvis Daily Script

on:
  schedule:
    - cron: '0 13 * * *'
  workflow_dispatch:

jobs:
  generate-and-send:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Generate script and send to QuickKick via LadyJayeBot
        working-directory: elvis-daily-script
        run: python generate_script.py
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          LADYJAYE_BOT_TOKEN: ${{ secrets.LADYJAYE_BOT_TOKEN }}
          QUICKKICK_CHAT_ID: ${{ secrets.QUICKKICK_CHAT_ID }}
